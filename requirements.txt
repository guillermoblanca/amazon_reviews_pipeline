# =============================================================================
# requirements.txt — Dependencias del pipeline de análisis de sentimiento
# =============================================================================

# Motor de procesamiento distribuido (incluye MLlib para ML)
pyspark==3.5.1

# Manipulación de datos y operaciones vectoriales
pandas==2.2.2
numpy==1.26.4

# Visualizaciones para el reporte de evaluación
matplotlib==3.9.0
seaborn==0.13.2

# Métricas adicionales de scikit-learn (para curvas ROC y clasificación)
scikit-learn==1.5.0

# Logging estructurado
loguru==0.7.2

# ── API de inferencia en producción ──────────────────────────────────────────
# Servidor ASGI de alto rendimiento (comparable a Flask pero async-native)
fastapi==0.111.0
# Servidor ASGI para producción
uvicorn[standard]==0.30.1

# ── Suite de tests ────────────────────────────────────────────────────────────
# Framework de tests
pytest==8.2.1
# Cliente HTTP para tests de integración (usado por FastAPI TestClient)
httpx==0.27.0
